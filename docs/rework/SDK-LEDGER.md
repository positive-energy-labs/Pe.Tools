# SDK Ledger — Pe.Revit.Sdk release-prep tracker

Every SDK defect/enhancement, with status. Any agent that finds an SDK issue ADDS A LINE here (status `open`). Phase exits require a review of this file. Statuses: `open` → `in-progress` → `fixed@<version|commit>` | `wontfix(<reason>)`.

## Ship-blockers (Phase 1)

| id | status | issue | where |
|---|---|---|---|
| S1 | fixed@worktree — EnsureUserPathStartsWith + LegacyPathEntriesForShimDir + PathsEqual + both call sites deleted; MSI owns PATH | `EnsureUserPathStartsWith` corrupts user PATH: REG_SZ overwrite kills REG_EXPAND_SZ `%VARS%`, rewrites entire PATH 2×/install, no WM_SETTINGCHANGE, fights MSI `Part="first"`. Fix: DELETE — MSI owns PATH (kernel's own comment says so). | InstallCommand.cs:829-845, called :238,:270 |
| S2 | fixed@worktree | **MSI × VersionedAddin transport DONE** in Pe.Revit.Sdk worktree (uncommitted; `source/Pe.Revit.Cli/MsiCommand.cs` only). Design (a) declarative WiX. MsiCommand's `VersionedAddin` case now lays the kernel-identical layout: stable loader shim (`addins\<year>\<name>\Pe.Revit.Loader.dll` + `loader.json` whose payloadRoot is `%LOCALAPPDATA%`-relative — the loader `ExpandEnvironmentVariables`-es it, and its parent == appBase so it feeds the new A3 `InstalledProduct.Open`) + rewritten `.addin` (via `InstallCommand.ShimAddinManifest`) + payload under `addin\versions\<version>\<year>` (per-year net48/net8 band via a replicated `MsiLoaderBinary`). `current.txt` is deliberately NOT a component — a single guarded deferred CA (`pe-pointer-guard.ps1`, type 1122, after InstallFiles, `NOT REMOVE`) writes it by a **dangling-pointer rule**: write MSI version iff pointer absent OR the version dir it names is gone. So a CLI-written pointer that still resolves is never regressed, and an MSI-only upgrade self-heals (MajorUpgrade drops only the MSI's own version-specific dir components; CLI-written version dirs were never components → survive upgrade + uninstall). Orphan-pointer case documented: MSI uninstall removes the shim → loader inert-on-missing (no crash); CLI dirs + pointer left behind; re-run `pe-revit install` to restore. Deliberate give-up: guard preserves rather than leapfrogs a still-valid *older* CLI pointer (MSI never fights the CLI, which is the update channel) — stricter than the brief's "absent-or-older" but honors the hard constraint with zero semver logic in a CA. VERIFIED: `dotnet build source/Pe.Revit.Cli` green; fixture manifest (VersionedAddin 2023+2025 + VersionedApp) → `pe-revit msi` builds an MSI; File/CustomAction/InstallExecuteSequence table dump + `msiexec /a` admin-extract confirm the exact layout and CA sequencing (RemoveExistingProducts@1401 < InstallFiles@4000 < PeWritePointer@4001, `NOT REMOVE`). Remaining is consumer-side (see C1 + kernel follow-ups): Pe.Tools still emits downgraded RevitAddin/Exe for the MSI lane. | MsiCommand.cs (VersionedAddin case + GuardScript + MsiLoaderBinary); consumer at Pe.Tools CreateInstallerModule.cs:238-255 |
| S3 | fixed@worktree — dev fallback deleted; dev.txt runs dev and propagates exit code, period | Shim `dev_then_installed` re-runs installed payload on ANY nonzero dev exit (double execution; Ctrl-C = nonzero). Fix: fall back only when dev launcher unlaunchable. | InstallCommand.cs:398-405 |
| S4 | fixed@worktree — shim consumes --installed/--dev, rebuilds args via EnableDelayedExpansion loop, quoted spaces preserved (known cmd ceiling: literal `!` in args mangled) | cmd `shift /1` doesn't affect `%*` — `--installed`/`--dev` leak into forwarded args. | InstallCommand.cs:374-385,:396,:401 |
| B1 | fixed@worktree — install.zip stage: sources resolve package-relative; dev + legacy bundle behavior unchanged | `stage:` source inside Install-kind package resolves to build-machine `.artifacts/stage` path (nonexistent on user machine). Treat as package-relative. | InstallCommand.cs:450-453 |

## Architecture (Phase 1)

| id | status | issue |
|---|---|---|
| A1 | fixed@worktree | **Swap demotion**: DONE in Pe.Revit.Sdk worktree (uncommitted). Deleted LoaderApplication `_swapEvent`/`_watcher`/`_debounce` fields, ExternalEvent+StartWatcher wiring, StartWatcher method, SwapHandler class; OnStartup now reads `current.txt` once → resolves versions/{v}/{year} → loads → Startup, with honest inert-on-missing log lines. PayloadHost simplified to single `_payloadDir` (net48) / single ALC (net8), side-by-side ActiveDirs list + newest-first + requester-dir probing deleted; loader-identity pinning + default-context fallthrough KEPT. `IsFirstLoad` removed from `PePayloadContext` (+ ctor param); `SlotDispatch.BeginRebind` deleted (swap-only); rest of SlotDispatch/Slots.g.cs/SlotCommandBinder KEPT (load-bearing first-load command indirection). Consumers fixed: HelloAddin AppCore (ribbon now unconditional), loader csproj Description. Loader builds clean net48+net8; HelloAddin + sample tests build clean. Rationale: dotnet/wpf#1700 + ILRepack short-name collapse — 7+ unfixable resource surfaces. |
| A2 | fixed@worktree | **Stop-stale-processes primitive**: DONE (uncommitted, InstallCommand.cs). Design refined from the ledger's original "restart" wording to **stop, do NOT respawn** — the product's own supervision (Revit's addin retry loop) is the respawner and resolves the NEW pointer on its next start; the SDK never spawns. (1) New optional manifest field `"stopOnUpdate": true` on payloads → `Payload.StopOnUpdate` (default false), parsed in `ParseManifest` (`JsonValueKind.True` only; absent/false/non-bool ⇒ false). Only honored for VersionedApp. (2) VersionedApp apply captures `previousVersion = ReadCurrent(appDir)` BEFORE the pointer flip; after staging + flip, if `StopOnUpdate && previousVersion is not null && previousVersion != version` it calls `StopStaleProcesses(versions/<previousVersion>)`. (3) `StopStaleProcesses`: `Process.GetProcesses()` → `MainModule.FileName` inside try/catch (access-denied/exited/bitness-mismatch skipped silently) → keep only images whose full path is under `versions/<oldVersion>/` (ordinal-ignore-case prefix, trailing-sep guarded) → `CloseMainWindow()` → `WaitForExit(2000)` grace → `Kill(entireProcessTree:true)` → `WaitForExit(2000)`; each is an action row `stop` with status `stopped-stale` (HasExited) or `stop-failed`. Never throws; `stop-failed` is NOT in the `failed` set (line ~285) so a survivor is reported, not fatal. (4) `--root` sandbox mode skips the stop pass entirely (`rootOverride is null` gate) for hermeticity — path-prefix already makes it safe, but tests never enumerate/kill by temp-root path. (5) Loader.Tests extended with a `stopOnUpdate` parse assertion (true/false/absent). Sequencing confirmed: CopyDir(stage) → WriteAllText(current) [flip] → PruneVersions → (Execute returns) → stop pass; a supervisor respawning mid-stop resolves the new pointer. VERIFIED live end-to-end with the real shipped CLI (not --root): published a dummy `sleeper.exe`, `install apply --version 0.0.1` (machine roots) staged it, started `versions/0.0.1/sleeper.exe` (PID 38460), `install apply --version 0.0.2 stopOnUpdate:true` advanced the pointer and emitted `[stopped-stale] ...versions\0.0.1\sleeper.exe` — process gone; new pointer 0.0.2. Negative: re-applying 0.0.2 (no advance) emitted NO stop row and the current process survived. `dotnet build source/Pe.Revit.Cli` green; Loader.Tests + Analyzers.Tests pass. |
| A3 | fixed@worktree | **Runtime layout API**: DONE (uncommitted). (1) InstallCommand apply now byte-faithfully copies the consumed `product.payloads.json` into `appBase` beside the receipt (one File.Copy after receipt write). (2) New public `Pe.Revit.Loader/InstalledProduct.cs` (both TFMs, regex JSON parse — no STJ on net48): `InstalledProduct.Discover(product,vendor,localAppDataOverride?)` → machine grammar `Combine(override??LOCALAPPDATA, Sanitize(vendor), Sanitize(product))`; `InstalledProduct.Open(appBase)` → the primitive that opens an explicit app base (what `--root` yields under `<root>/app`, and what the loader derives from payloadRoot's parent — both Discover and LoaderApplication call it). `.Resolve(payloadName, revitYear?)` → `PayloadLocation(name,type,currentVersion?,versionDirectory,entryPath?)` reading that payload's own current.txt: VersionedApp=`bin/<name>/versions/<v>/`+entry(`entry??<name>.exe`), VersionedAddin=`addin/versions/<v>[/<year>]/`(entryPath null, entry is a class), Cli/Exe=`bin/<name>/`, LooseFiles=`<target??name>/`, PathShim=`shims/<name>.cmd`. Null on unknown payload / missing current.txt / missing dir — never throws. `.ShimsDirectory`, `.ReceiptVersion` (receipt `releaseVersion`), `.Lane` (PE_LANE=installed\|dev wins, else any `*.dev.txt` in shims ⇒ dev, else installed). (3) `PePayloadContext` gained `Version` (string?) + `Deployment` (InstalledProduct?), additive ctor params (default null), populated in LoaderApplication (reads current.txt version + Open(parent-of-payloadRoot)); `CreateSelfHosted` unchanged ⇒ HelloAddin/template consumers needed no edits. **Finding:** the `--root` layout (`<root>/app`) can't be reproduced by Discover's LOCALAPPDATA override (machine grammar nests vendor/product), so `Open(appBase)` is the tested primitive; Discover is the machine convenience delegating to it. |
| A4 | fixed@worktree | **Round-trip contract test**: DONE (uncommitted). New `source/Pe.Revit.Loader.Tests` (net8.0-windows bare-Exe harness, mirrors Analyzers.Tests; run via `dotnet run`). InternalsVisibleTo on Pe.Revit.Cli lets it drive `InstallCommand.Run("apply", --root <temp>)` in-process over a generated fixture manifest (VersionedApp + VersionedAddin@2025 + PathShim; sources written to temp at runtime). Copies the loader net8.0-windows dll into `loader/net8.0-windows/` so `LoaderBinary(2025)` resolves. Asserts: manifest byte-faithfully copied into root; every payload resolves to the exact dir/entry the installer wrote; current.txt version == receipt version; unknown payload → null; Discover/Open with no install → null; deleted current.txt → null (not throw). Fails if `versions`/`current.txt`/`bin/<name>`/`addin/versions/<v>/<year>` grammar drifts on either side. Cli payload skipped (self-copy of a Roslyn-laden bin — not cheap here). |
| A5 | fixed@worktree | **Version SoT**: DONE (uncommitted). Both readers now take the top-level `"version"` field of `product.payloads.json` as PRIMARY, with legacy `pe-version.json` kept as a fallback (existing consumers not broken this bump). Authority order (identical on both sides): explicit override → manifest `"version"` → pe-version.json. (1) `Pe.Revit.Common.props` (~:94-107): added a `PePayloadsManifest` probe + regex-in-MSBuild read of the manifest version BEFORE the pe-version.json read (same `(?<="version"\s*:\s*")[^"]+` technique); explicit `Version`/`VersionPrefix` precedence untouched; doc comment rewritten to the new order. (2) `RepoContext.cs`: `Find` now treats `product.payloads.json` OR `pe-version.json` as a repo-root marker (needed so manifest-only checkouts still locate Root); `ResolveVersion` reads `ManifestFile` version first then `VersionFile`; new `VersionSource()` returns which file won; extracted `ReadVersionField` (STJ, tolerant). (3) Migrated the sample (`samples/HelloAddin/product.payloads.json` gained `"version":"0.1.0-beta.1"`, deleted `samples/HelloAddin/pe-version.json`) and the template (new `templates/pe-revit-solution/product.payloads.json` with a version field, deleted that template's `pe-version.json` — NEW consumers get version-in-manifest, no pe-version.json). SDK repo itself has no root manifest → deliberately left on pe-version.json (per brief). Consumer-facing strings/guides updated (version/release/install missing-version messages, `new-addin.md`/`release.md`, Program.cs help, Templates.csproj comment); doctor's `version-file` check became manifest-aware `version-source` (would have false-failed manifest-only consumers). VERIFIED: `dotnet build source/Pe.Revit.Cli` green; `Pe.Revit.Loader.Tests` extended with an A5 fixture (manifest-only → returns manifest version; pe-version.json-only → falls back; both → manifest wins; explicit override wins) — passes; props proven end-to-end by packing the SDK to the local feed, pinning HelloAddin to it, and evaluating `Hello.Addin` → `Version=0.1.0-beta.1` from the manifest with no pe-version.json, built dll `InformationalVersion=0.1.0-beta.1`. **Deferred (future line F1):** git-tag/`git describe`-driven version for 0-file releases. | props :94-107; RepoContext.cs Find/ResolveVersion |
| F1 | open | **Git-tag version authority (deferred from A5)**: add `git describe --tags` (or an annotated-tag read) as a version-resolution tier so a tagged release needs zero version-carrying files. Wire into both readers (Common.props via a `$(git ...)` exec or a pre-props CLI stamp, and `RepoContext.ResolveVersion`). Decide precedence vs the manifest field (likely: explicit override → tag-on-HEAD → manifest → pe-version.json). Deliberately deferred; the manifest field is the SoT for now. |
| A8 | open | **MSI VersionedApp `current.txt` has the same coexistence hazard S2 fixed for VersionedAddin, but is still a plain File component** (MsiCommand.cs VersionedApp case: `<File ... Name="current.txt">`). On MSI repair it restores to the MSI version (regresses a newer CLI pointer); on MSI upgrade MajorUpgrade+reinstall can regress it. Low urgency (the shipped VersionedApp is the host, updated via CLI), but for true parity it should use the same guarded pointer CA. Fix: generalize S2's `pe-pointer-guard.ps1` CA to also cover each VersionedApp payloadRoot (`bin/<name>`), and drop current.txt from that component. Discovered during S2. |
| A9 | fixed@worktree — LoaderBinary internal, MsiLoaderBinary deleted, MsiCommand calls through; year→TFM map single-sourced | **`MsiCommand.MsiLoaderBinary` duplicates the kernel's private `InstallCommand.LoaderBinary`** (TFM-band → loader dll) — two copies of the year→net48/net8 map that must move together (both need a net10 band at Revit 2027). Introduced by S2 because `LoaderBinary` is `private` in InstallCommand (which a parallel agent owned during S2; couldn't widen it). Fix: make `InstallCommand.LoaderBinary` `internal` and delete `MsiCommand.MsiLoaderBinary`. Discovered during S2. |
| A6 | fixed@worktree | **Pin-drift guard** (full unification of the two SDK pins deliberately deferred — this is the honest guard, not a re-plumb of how the CLI ships). DONE (uncommitted). `RepoContext.WarnOnSdkPinDrift()`: at the start of `install`/`msi`/`release` (wired once in `Program.cs` before dispatch, so `MsiCommand.cs` — fenced — is untouched), when a `global.json` exists at the repo root, it regex-reads the `Pe.Revit.Sdk` msbuild-sdks pin and compares it to the running CLI's own `AssemblyInformationalVersion` (via `CliVersion()`, which strips any `+<commit>` build metadata). On mismatch it prints exactly one stderr line — `pe-revit <cliver> != Pe.Revit.Sdk <pin> in global.json — bump .config/dotnet-tools.json` — and continues (never blocks). Silent when not in a repo / no global.json / no pin / versions match. Best-effort (swallows IO); ~35 lines, no new deps, regex-field parse like the rest. VERIFIED with the freshly built CLI (informational `0.1.0-beta.14`): mismatched pin → the exact warning then the command proceeds; matched pin → 0 warnings; manifest-only dir with no global.json → 0 warnings. **Note:** this catches drift but does not eliminate the second pin — `.config/dotnet-tools.json` still exists and the human must bump it. True single-pin (dotnet-tools pin dies / CLI ships inside the SDK package) remains future work; kept as F2. | RepoContext.cs WarnOnSdkPinDrift/CliVersion; Program.cs dispatch |
| F2 | open | **True single SDK pin (deferred from A6)**: eliminate the `.config/dotnet-tools.json` pe.revit.cli pin entirely — either ship the CLI inside the `Pe.Revit.Sdk` package (resolved via the msbuild-sdks pin, invoked through a target/wrapper) or generate the tool-manifest pin from `global.json` at restore. A6 only *warns* on drift; this removes the drift surface. |
| A7 | fixed@worktree — died with S1 deletion | Reverse leak: `LegacyPathEntriesForShimDir` hardcodes Pe.Tools names (`bin/pea`, `bin/pe-dev`) inside the product-agnostic kernel. Dies with S1 deletion; verify. | 

## Polish (Phase 1, low)

| id | status | issue |
|---|---|---|
| P1 | fixed@worktree — sibling tag dirs deleted before extract; cache dir deleted after successful release apply | Release temp dirs never cleaned: `%TEMP%/pe-revit/release/<tag>` accumulates one extract per tag forever (InstallCommand.cs:507-525). |
| P2 | fixed@worktree — Program.cs help updated | `pe-revit install --release` help text stale (bundle/addin-only wording). |
| P3 | fixed@worktree — NormalizePayloadVersion applied once on write when versioned payloads present | Version dir written raw but resolved normalized (`NormalizePayloadVersion`) — normalize on write (InstallCommand.cs:141,148). |
| P4 | fixed@worktree — static removed, ReleasePackage threaded through resolver overload | `_releasePackage` mutable static in otherwise-stateless kernel — thread through as parameter (InstallCommand.cs:442). |
| P8 | fixed@worktree — Loader.Tests wired into smoke.ps1 as Check 1.25 | `source/Pe.Revit.Loader.Tests` (A4 round-trip) is not yet wired into `scripts/smoke.ps1` — add a `dotnet run --project` check beside the analyzer-tests check (Check 1.2) so the layout-grammar contract runs in CI. Bare-Exe harness (throws on failure), same as Analyzers.Tests; run via `dotnet run`, not `dotnet test`. |
| P5 | open | Cross-product stale loader registrations (`Assembly with same name is already loaded` from old HelloAddin) — installer cleanup story. |
| P6 | open | SDK repo hygiene: beta.11–14 shipped from uncommitted worktree (fix in Phase 0 by committing); HelloAddin sample polluted with hot-swap markers. |
| P7 | fixed@worktree | Stale "live swap" wording post-A1 reworded (uncommitted). Fixed: `LiveCommand.cs:269` + `:1065` (identical installed-lane hint) and `:1062` comment → `install apply` now reads "staged: the new version takes effect at the next Revit start; this session keeps the one it loaded". `InstallCommand.cs:89` ("live-swappable addin payloads" → "versioned addin payloads") and `:156-161` comment ("live-swap via the loader's current.txt watch" → "the loader reads current.txt once at startup (no live swap): open sessions stay pinned until Revit restarts"). Enhanced the printed VersionedAddin note (~:310) to state staged-until-next-start + open sessions keep their version — the "does not overwrite loaded payload DLLs" clause was already true, kept. **`Program.cs`: no change needed** — the ledger's `:109` reference had drifted; P2 already cleaned the install help, and the only remaining "hot-reload" wording (`:122`) is the dev `live` attached-bridge hot-reload, which is genuine and unrelated to the demoted loader swap. Install-kind release note ("Running processes may continue on the old version until they are restarted") was already correct, kept. |
| P10 | open | **Re-applying the SAME VersionedApp version while its process runs fails the copy** (pre-existing; surfaced during A2 live verification, NOT introduced by A2). `install apply` with an unchanged version re-`CopyDir`s the source over `versions/<v>/`, whose running exe holds a lock on its own dll → `File.Copy` throws IOException → the payload is reported `[failed]` and sets the overall install failed. Harmless in normal use (versions are monotonic ⇒ always a fresh dir), but an operator re-running the same apply against a live app sees a spurious failure. Fix options: detect "already current + verify() ok" and treat re-apply of the current version as a no-op/skip, or make the copy best-effort over a locked current dir. Low priority. Discovered during A2. |
| P9 | open | Residual `pe-version.json` wording post-A5: the primary CLI/guide strings were updated, but some SDK-target hints still name pe-version.json as THE authority — `Pe.Revit.Sdk/Sdk/Pe.Revit.Explain.targets:30` ("authority: pe-version.json"), `Pe.Revit.Package.targets:244,:246` (pack-forbidden hint), and `McpCommand.cs:22` / `MsiCommand.cs:78` (the latter fenced). Reword to "product.payloads.json version field (pe-version.json fallback)". Low priority — cosmetic; the readers already prefer the manifest. Discovered during A5. |

## Consumer-side items tracked here because the SDK gap causes them

| id | status | issue |
|---|---|---|
| C1 | open | Pe.Tools `CreateInstallerModule.cs` hand-maintains TWO C# manifests duplicating `product.payloads.json` (drifted: MSI=RevitAddin/Exe, peco dropped, slug ×3). Delete after S2/A5; build becomes source-rewrite transform only. |
| C2 | open | `Pe.Shared.Product` prune after A3: descriptor model (`PeAppRuntimeDeploymentDescriptor` — zero writers; `RevitDeploymentIdentity` models pre-loader layout), `PeaLauncherContent` (duplicate of SDK ShimContent — two competing pea.cmd generators ship today), layout constants. **Also (A1 ripple):** the SDK removed `PePayloadContext.IsFirstLoad` at the next bump. Pe.Tools consumes it in `ButtonRegistry`/`AppCore` (Phase 2) — those `if (context.IsFirstLoad)` ribbon branches must become unconditional when Pe.Tools moves to the beta with A1's contract change, or the payload won't compile against the new loader. |
| C3 | open | `ThemeManager.cs:55` hardcodes pack URI naming pre-merge assembly `Pe.Revit.Ui` (works single-load by WPF fallback luck). Fix regardless of demotion. |
| C4 | open | Scripting `ScriptAssemblyLoadService` first-simple-name-match assembly resolution (:158-165,:199-220) — mostly mooted by A1 demotion (single era), but Location-match is still more correct; decide during Phase 2. |

## Review-gate findings (pre-beta.15 adversarial review of the Phase-1 diff)

| id | status | issue |
|---|---|---|
| R1 | fixed@worktree | InstalledProduct.ParsePayloads flat-brace regex dropped payloads with `{`/`}` in string values (pea/peco `{root}` dev commands) — Resolve returned null for the flagship manifest. Fixed with string-aware brace-depth scanner; braced-payload regression test added to Loader.Tests. |
| R2 | fixed@worktree | smoke.ps1 Check 24 asserted doctor id `version-file`, renamed to `version-source` by A5 — smoke was red. One-line fix. |
| R3 | fixed@worktree | MSI never laid product.payloads.json nor a runtime install.receipt.json into the app root, so InstalledProduct.Open returned null on MSI installs. Both now emitted as File components (receipt carries releaseVersion + channel:msi). |
| R4 | fixed@worktree | MSI wrote raw version where CLI writes NormalizePayloadVersion — channels could lay different versions/<v> dirs. MSI now calls the same (internal) normalizer. |
| R5 | open | LoaderApplication.OnStartup returns Result.Failed (Revit error dialog) on missing/corrupt loader.json — the inert-never-crash philosophy covers staged-missing-version but not a corrupt shim. Low priority. |
| R6 | open | Program.cs pin-drift guard walks RepoContext.Find() redundantly before dispatch (each ship command Finds again). Cosmetic. |
